# Environment Variables for Cloud Spark Backend

# MongoDB Connection String
# Replace with your actual MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/cloudspark_db

# JWT Secret Key
# Replace with a strong, random secret key
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_very_strong_and_secret_jwt_key_replace_me

# Server Port (Optional, defaults to 5000 if not set)
# PORT=5001
